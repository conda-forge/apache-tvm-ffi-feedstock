schema_version: 1

context:
  name: apache-tvm-ffi
  version: "0.1.10"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace("-", "_") }}-${{ version }}.tar.gz
  sha256: 974c208766c304c780c17c6d405449e862f83b22c7b6b2b8c28b29d55a806ae3

build:
  number: 0
  skip:
    - win
  script:
    - export SETUPTOOLS_SCM_PRETEND_VERSION=${{ version }}
    - python -m pip install . -vv

requirements:
  build:
    - if: build_platform != host_platform
      then:
        - python
        - cross-python_${{ host_platform }}
        - cython
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
    - cmake
    - ninja
    - make
  host:
    - python
    - pip
    - scikit-build-core >=0.10.0
    - cython >=3.0
    - setuptools-scm
  run:
    - python
    - typing-extensions >=4.5

tests:
  - python:
      imports:
        - tvm_ffi
      pip_check: true

about:
  homepage: https://github.com/apache/tvm-ffi
  summary: TVM FFI - Foreign Function Interface for ML systems
  description: |
    TVM FFI is a lightweight foreign function interface library designed for
    machine learning systems. It provides efficient cross-language bindings
    between Python and C++.
  license: Apache-2.0
  license_file: LICENSE
  repository: https://github.com/apache/tvm-ffi

extra:
  recipe-maintainers:
    - iamthebot
